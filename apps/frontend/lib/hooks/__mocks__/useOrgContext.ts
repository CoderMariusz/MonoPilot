/**
 * Mock for useOrgContext Hook
 * Story: 01.2 - Settings Shell: Navigation + Role Guards
 *
 * Used by tests to mock organization context
 */

import { vi } from 'vitest'

export const useOrgContext = vi.fn()
