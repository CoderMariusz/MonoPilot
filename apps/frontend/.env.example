# ============================================================================
# MonoPilot Frontend Environment Configuration
# ============================================================================
# This file documents environment variables used by the Next.js frontend
# For local development, copy to .env.local in the project root

# ============================================================================
# Supabase Configuration (Public)
# ============================================================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
NEXT_PUBLIC_USE_MOCK_DATA=false
NEXT_PUBLIC_APP_URL=https://your-domain.com
NEXT_PUBLIC_APP_ENV=development

# ============================================================================
# Server-Side Only Variables (Not exposed to browser)
# ============================================================================
# See root .env.example for full list of server-side environment variables

# Note: All environment variables should be configured in the root .env.local
# This file is for documentation purposes only
