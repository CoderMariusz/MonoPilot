#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Pre-commit hook disabled - use pre-push instead for better performance
# Linting and type-checking happens on pre-push and in CI/CD
echo "✓ Pre-commit hook passed (linting deferred to pre-push)"
